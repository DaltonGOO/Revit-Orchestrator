{
  "name": "orchestrator.suggest_workflows",
  "adapter": "workflow",
  "description": "Analyzes execution history to detect recurring tool-call patterns and suggests named workflows for them.",
  "version": "1.0.0",
  "tags": ["observability", "workflow", "suggestions"],
  "permissions": {
    "mode": "read"
  },
  "side_effects": [],
  "parameters": {
    "type": "object",
    "properties": {
      "lookback_days": {
        "type": "integer",
        "description": "Number of days to look back in the audit log (default: 30)",
        "default": 30
      },
      "min_occurrences": {
        "type": "integer",
        "description": "Minimum number of times a pattern must occur (default: 3)",
        "default": 3
      }
    }
  },
  "returns": {
    "type": "object",
    "properties": {
      "suggestions": {
        "type": "array",
        "description": "List of workflow suggestions"
      },
      "patterns_found": { "type": "integer" }
    }
  },
  "examples": [
    {
      "description": "Get workflow suggestions from the last 30 days",
      "args": { "lookback_days": 30, "min_occurrences": 3 }
    }
  ]
}
